# Tasks — Refs #42

- [x] A-T1 Audit current main, governance, source contracts; create Issue (A1–A8).
- [x] A-T2 Bounded owner catalog/detail/preview, shared selected RAG and limiter (A2,A5,A7). Backend owner scoping and bounded DTOs are covered by `DocumentApiTests` and the browser isolation run.
- [x] A-T3 Compact responsive Library + existing image results (A1,A6). Desktop three-pane layout and tablet/mobile drawers are covered by `Library.test.tsx` and Chromium screenshots.
- [x] A-T4 Upload frozen intent/delete confirmation and accessible dialogs (A3,A4,A7). Retry retains the exact request intent; `Modal` traps focus and restores the trigger.
- [x] A-T5 Meaningful API/UI/security tests and actual browser evidence (A1–A7). Frontend, backend, dependency, audit and browser evidence are recorded in `test-evidence/`.
- [ ] A-T6 Full checks, scoped commit/push/exact SHA/CI and close (A8). Code checks are complete; final commit/push and Issue state remain pending while the configured AI provider is unavailable.
