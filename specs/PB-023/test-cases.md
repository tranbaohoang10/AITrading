# PB-023 test cases

- Anonymous, forged Bearer, missing/wrong/duplicate expected-account, revoked and
  cross-owner sessions across representative private resource families.
- Missing/wrong/replayed CSRF, untrusted Origin, unsupported method/media type,
  duplicate JSON keys, trailing tokens, scalar coercion and body/header/path bounds.
- SQL/meta/control/Unicode/HTML/script/formula/shell/path/URL payloads remain data or
  are rejected; no schema deletion, browser execution, filesystem write or fetch.
- Brute-force and per-owner read/write/AI/job quotas, Retry-After and independent
  owner budgets; concurrent replay/stale/delete/restart consistency.
- Headers/cookies/cache/CSP/referrer/permissions/cross-origin behavior and bounded
  redacted 4xx/5xx responses/logs.
- Dependency locks, wrapper hash, npm/OSV advisories, exact secret and credential
  literal scan, fixed provider/process endpoints and clean child environment.
- Full functional/integration/regression suites after every concrete fix.
