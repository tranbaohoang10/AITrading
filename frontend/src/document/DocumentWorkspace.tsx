import { useEffect,useState,type FormEvent } from 'react';import { useAuth } from '../auth/AuthContext';import { buttonClass,inputClass } from '../auth/AuthForm';import * as api from './api';
export function DocumentWorkspace(){const auth=useAuth(),[docs,setDocs]=useState<api.Document[]>([]),[target,setTarget]=useState(''),[title,setTitle]=useState(''),[file,setFile]=useState<File|null>(null),[question,setQuestion]=useState(''),[answer,setAnswer]=useState<api.Answer|null>(null),[busy,setBusy]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);return api.list(auth?.user.id).then(setDocs).catch(()=>setError('Could not load private documents.')).finally(()=>setLoading(false))};useEffect(()=>{void load()},[]);async function upload(e:FormEvent){e.preventDefault();if(!file||busy)return;const current=docs.find(d=>d.id===target);setBusy(true);setError('');try{await api.upload(file,title,current?.currentVersion??0,current?.id??null,crypto.randomUUID(),auth?.user.id);setTitle('');setFile(null);setTarget('');await load()}catch{setError('Upload was rejected or its outcome is uncertain. Reload before retrying.')}finally{setBusy(false)}}async function remove(d:api.Document){if(busy)return;setBusy(true);setError('');try{await api.remove(d.id,d.currentVersion,auth?.user.id);if(target===d.id)setTarget('');await load()}catch{setError('Delete was rejected or the document changed. Reload before retrying.')}finally{setBusy(false)}}async function ask(e:FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError('');setAnswer(null);try{setAnswer(await api.ask(question,auth?.user.id))}catch{setError('RAG request failed. No answer or citation was fabricated.')}finally{setBusy(false)}}return <section aria-label="Private document library" className="h-full overflow-y-auto bg-slate-950 p-4 text-slate-100 sm:p-6"><h1 className="text-xl font-semibold">Private document library</h1><p className="mt-1 text-xs text-slate-400">Owned text/PDF evidence · files are data, never instructions · no URLs, tools or code execution</p>{error&&<p role="alert" className="my-4 text-rose-200">{error}</p>}{busy&&<p role="status" className="mt-3 text-sm text-slate-400">Processing private document request…</p>}<div className="mt-5 grid gap-6 xl:grid-cols-2"><form onSubmit={upload} className="space-y-3" aria-label="Upload private document"><h2 className="font-semibold">Upload synthetic/private evidence</h2><select className={inputClass} aria-label="Document version target" value={target} onChange={e=>setTarget(e.target.value)}><option value="">Create a new document</option>{docs.map(d=><option key={d.id} value={d.id}>Replace {d.title} · v{d.currentVersion}</option>)}</select><input className={inputClass} aria-label="Document title" required maxLength={160} value={title} onChange={e=>setTitle(e.target.value)}/><input aria-label="Document file" type="file" required accept=".txt,text/plain,.pdf,application/pdf" onChange={e=>setFile(e.target.files?.[0]??null)}/><p className="text-xs text-slate-400">TXT or PDF only, maximum 2 MiB; PDF maximum 50 pages and extracted text 100 KiB.</p><button className={buttonClass} disabled={busy}>{target?'Upload next version':'Upload document'}</button>{loading?<p role="status" className="text-sm text-slate-400">Loading private documents…</p>:docs.length===0?<p className="text-sm text-slate-400">No private documents yet.</p>:null}<ul aria-label="Owned documents" className="divide-y divide-slate-800">{docs.map(d=><li key={d.id} className="flex items-center justify-between gap-3 py-3"><span><span className="font-medium">{d.title}</span><span className="ml-2 text-xs text-slate-500">v{d.currentVersion}</span></span><button type="button" disabled={busy} onClick={()=>void remove(d)} className="text-xs text-rose-300">Delete</button></li>)}</ul></form><form onSubmit={ask} className="space-y-3" aria-label="Ask private documents"><h2 className="font-semibold">Ask with citations</h2><textarea className={`${inputClass} min-h-24`} aria-label="Document question" required maxLength={1000} value={question} onChange={e=>setQuestion(e.target.value)}/><button className={buttonClass} disabled={busy}>Retrieve and answer</button>{answer&&<article aria-label="RAG answer" className="space-y-3 border-t border-slate-800 pt-4"><p>{answer.answer}</p>{answer.provider&&<p className="text-xs text-slate-500">{answer.provider} · {answer.model}</p>}<ol className="space-y-2">{answer.citations.map((c,i)=><li key={`${c.documentId}:${c.version}:${c.chunkIndex}`} className="border border-slate-800 p-3 text-sm"><strong>[C{i+1}] {c.title} · v{c.version}{c.pageNumber?` · page ${c.pageNumber}`:''}</strong><p className="mt-1 whitespace-pre-wrap break-words text-slate-300">{c.excerpt}</p></li>)}</ol><p className="text-xs text-slate-400">Private-source research answer; verify citations. Not financial advice or a profitability guarantee.</p></article>}</form></div></section>}
