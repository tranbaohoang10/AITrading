import { useEffect, useRef, useState, type ReactNode } from 'react'
import { Icon } from './Icon'
import { captureSvgRegion, sendChartCaptureToAssistant } from '../market/chartCapture'
import type { DrawingTool, MagnetMode } from '../market/chartTypes'

type ToolItem = { tool: DrawingTool; label: string; icon: string; disabled?: boolean }
type ToolGroup = { id: string; label: string; items: ToolItem[] }
const groups: ToolGroup[] = [
  { id: 'lines', label: 'Lines & Channels', items: [{ tool: 'trend', label: 'Trend Line', icon: 'trend' }, { tool: 'horizontal', label: 'Horizontal Line', icon: 'horizontal' }, { tool: 'ray', label: 'Ray', icon: 'ray' }, { tool: 'parallelChannel', label: 'Parallel Channel', icon: 'parallelChannel' }, { tool: 'vertical', label: 'Vertical Line', icon: 'vertical' }, { tool: 'extended', label: 'Extended Line', icon: 'trend' }] },
  { id: 'fib', label: 'Fibonacci', items: [{ tool: 'fibRetracement', label: 'Fib Retracement', icon: 'fib' }, { tool: 'fibExtension', label: 'Trend-based Fib Extension', icon: 'fib' }] },
  { id: 'patterns', label: 'Patterns', items: [{ tool: 'xabcd', label: 'XABCD Pattern · Coming later', icon: 'pattern', disabled: true }, { tool: 'abcd', label: 'ABCD Pattern · Coming later', icon: 'pattern', disabled: true }, { tool: 'headAndShoulders', label: 'Head & Shoulders · Coming later', icon: 'pattern', disabled: true }, { tool: 'elliottImpulse', label: 'Elliott Impulse · Coming later', icon: 'pattern', disabled: true }, { tool: 'elliottCorrection', label: 'Elliott Correction · Coming later', icon: 'pattern', disabled: true }, { tool: 'trianglePattern', label: 'Triangle Pattern · Coming later', icon: 'pattern', disabled: true }] },
  { id: 'shapes', label: 'Shapes', items: [{ tool: 'rectangle', label: 'Rectangle', icon: 'rectangle' }, { tool: 'brush', label: 'Brush', icon: 'brush' }, { tool: 'ellipse', label: 'Ellipse', icon: 'shapes' }, { tool: 'polyline', label: 'Polyline', icon: 'polyline' }, { tool: 'triangle', label: 'Triangle', icon: 'triangle' }] },
  { id: 'text', label: 'Text / Annotation', items: [{ tool: 'text', label: 'Text', icon: 'text' }, { tool: 'note', label: 'Note', icon: 'text' }, { tool: 'callout', label: 'Callout', icon: 'text' }, { tool: 'priceNote', label: 'Price Note', icon: 'priceNote' }] },
  { id: 'position', label: 'Position / Risk', items: [{ tool: 'longPosition', label: 'Long Position', icon: 'longPosition' }, { tool: 'shortPosition', label: 'Short Position', icon: 'shortPosition' }] },
  { id: 'measure', label: 'Measure', items: [{ tool: 'ruler', label: 'Ruler', icon: 'ruler' }, { tool: 'priceRange', label: 'Price Range', icon: 'ruler' }, { tool: 'dateRange', label: 'Date Range', icon: 'ruler' }, { tool: 'datePriceRange', label: 'Date & Price Range', icon: 'ruler' }] },
]
const toolButton = 'icon-tool grid h-8 w-8 shrink-0 place-items-center rounded-md text-slate-500 transition hover:bg-slate-800 hover:text-slate-100 focus-visible:ring-2 focus-visible:ring-slate-300 disabled:cursor-not-allowed disabled:opacity-35'

export function ChartToolsRail({ selected: controlled, onSelect, hasDrawings = false, onClear, magnet = 'off', onMagnetChange, stayInMode = false, onToggleStay, allDrawingsLocked = false, onToggleLockAll, allDrawingsHidden = false, onToggleHideAll }: {
  selected?: DrawingTool; onSelect?: (tool: DrawingTool) => void; canDelete?: boolean; hasDrawings?: boolean; onDelete?: () => void; onClear?: () => void
  magnet?: MagnetMode; onMagnetChange?: (mode: MagnetMode) => void; stayInMode?: boolean; onToggleStay?: () => void; onObjectTree?: () => void
  allDrawingsLocked?: boolean; onToggleLockAll?: () => void; allDrawingsHidden?: boolean; onToggleHideAll?: () => void
} = {}) {
  const containerRef = useRef<HTMLElement>(null)
  const [local, setLocal] = useState<DrawingTool>('cursor'), [openMenu, setOpenMenu] = useState<string | null>(null), [confirmClear, setConfirmClear] = useState(false)
  const [lastUsed, setLastUsed] = useState<Record<string, DrawingTool>>({ cursor: 'cursor', lines: 'trend', fib: 'fibRetracement', patterns: 'abcd', shapes: 'rectangle', text: 'text', position: 'longPosition', measure: 'ruler' })
  const selected = controlled ?? local
  const choose = (tool: DrawingTool, group?: string) => { if (onSelect) onSelect(tool); else setLocal(tool); if (group) setLastUsed(value => ({ ...value, [group]: tool })); setOpenMenu(null); setConfirmClear(false) }
  useEffect(() => {
    const outside = (event: MouseEvent) => { if (!containerRef.current?.contains(event.target as Node)) { setOpenMenu(null); setConfirmClear(false) } }
    const escape = (event: KeyboardEvent) => { if (event.key === 'Escape') { setOpenMenu(null); setConfirmClear(false) } }
    document.addEventListener('pointerdown', outside); document.addEventListener('keydown', escape)
    return () => { document.removeEventListener('pointerdown', outside); document.removeEventListener('keydown', escape) }
  }, [])
  const flyout = (id: string, label: string, items: ToolItem[]) => openMenu === id && <div className="drawing-flyout absolute left-0 top-10 z-50 w-60 rounded-lg border border-slate-700 bg-slate-900 p-1.5 shadow-2xl sm:left-11 sm:top-0"><p className="px-2 py-1 text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500">{label}</p>{items.map(item => <button key={item.label} type="button" disabled={item.disabled} aria-label={item.label} title={item.disabled ? 'Coming later' : item.label} onClick={() => choose(item.tool, id)} className={`flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] ${selected === item.tool ? 'bg-slate-700 text-white' : 'text-slate-300 hover:bg-slate-800 hover:text-white'} disabled:text-slate-600`}><Icon name={item.icon} className="h-3.5 w-3.5"/><span>{item.label}</span></button>)}</div>
  return <aside ref={containerRef} aria-label="Chart tools" className="chart-tools flex h-10 shrink-0 items-center gap-0.5 overflow-x-auto border-b border-slate-800 bg-slate-925 px-1 sm:h-auto sm:w-11 sm:flex-col sm:overflow-visible sm:border-b-0 sm:border-r sm:px-0 sm:py-1.5">
    <ToolCell label="Cursor" mainTitle={lastUsed.cursor === 'crosshair' ? 'Crosshair' : 'Cursor'} active={selected === 'cursor' || selected === 'crosshair'} main={lastUsed.cursor === 'crosshair' ? 'crosshair' : 'cursor'} onClick={() => choose(lastUsed.cursor, 'cursor')} onMenu={() => setOpenMenu(openMenu === 'cursor' ? null : 'cursor')} menu={flyout('cursor', 'Cursor', [{ tool: 'cursor', label: 'Cursor', icon: 'cursor' }, { tool: 'crosshair', label: 'Crosshair', icon: 'crosshair' }])} />
    <ToolCell label="AI Capture" active={selected === 'aiCapture'} main="aiCapture" onClick={() => choose('aiCapture')} />
    {groups.map(group => { const active = group.items.some(item => item.tool === selected), main = group.items.find(item => item.tool === lastUsed[group.id] && !item.disabled) ?? group.items.find(item => !item.disabled) ?? group.items[0]; return <ToolCell key={group.id} label={group.label} mainTitle={main.label} active={active} main={main.icon} onClick={() => choose(main.tool, group.id)} onMenu={() => setOpenMenu(openMenu === group.id ? null : group.id)} menu={flyout(group.id, group.label, group.items)} /> })}
    <div className="my-1 hidden h-px w-7 bg-slate-800 sm:block" />
    <div className="group relative h-8 w-11 shrink-0"><button type="button" aria-label="More drawing controls" aria-pressed={openMenu === 'more'} title="More drawing controls" data-tooltip="More" onClick={() => { setOpenMenu(openMenu === 'more' ? null : 'more'); setConfirmClear(false) }} className={`${toolButton} mx-auto ${openMenu === 'more' ? 'bg-slate-800 text-slate-100' : ''}`}><Icon name="more" className="h-4 w-4" /></button>{openMenu === 'more' && <div className="drawing-flyout absolute left-0 top-10 z-50 w-56 rounded-lg border border-slate-700 bg-slate-900 p-1.5 shadow-2xl sm:left-11 sm:top-auto sm:bottom-0"><p className="px-2 py-1 text-[9px] font-semibold uppercase tracking-[.14em] text-slate-500">Utilities / More</p><p className="px-2 py-1 text-[9px] uppercase tracking-wide text-slate-600">Magnet</p><div className="flex gap-1 px-1 pb-1">{(['off', 'weak', 'strong'] as MagnetMode[]).map(mode => <button key={mode} type="button" aria-pressed={magnet === mode} onClick={() => onMagnetChange?.(mode)} className={`min-h-7 rounded px-2 text-[10px] capitalize ${magnet === mode ? 'bg-slate-700 text-white' : 'text-slate-400 hover:bg-slate-800'}`}>{mode}</button>)}</div><button type="button" aria-pressed={stayInMode} disabled={!onToggleStay} onClick={onToggleStay} className="flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="repeat" className="h-3.5 w-3.5" />Stay in Drawing Mode</button><button type="button" disabled={!hasDrawings} onClick={() => choose('eraser')} className="flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="eraser" className="h-3.5 w-3.5" />Eraser</button><button type="button" disabled={!hasDrawings || !onToggleLockAll} aria-pressed={allDrawingsLocked} onClick={onToggleLockAll} className="flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name={allDrawingsLocked ? 'lock' : 'unlock'} className="h-3.5 w-3.5" />Lock All Drawings</button><button type="button" disabled={!hasDrawings || !onToggleHideAll} aria-pressed={allDrawingsHidden} onClick={onToggleHideAll} className="flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name={allDrawingsHidden ? 'eyeOff' : 'eye'} className="h-3.5 w-3.5" />Hide All Drawings</button>{confirmClear ? <div className="mt-1 rounded-md border border-rose-900/80 bg-rose-950/30 p-1.5"><p className="px-1 text-[10px] text-rose-200">Remove all drawings?</p><div className="mt-1 flex gap-1"><button type="button" aria-label="Confirm remove all drawings" onClick={() => { onClear?.(); setOpenMenu(null); setConfirmClear(false) }} className="rounded bg-rose-700 px-2 py-1 text-[10px] font-semibold text-white">Remove</button><button type="button" aria-label="Cancel remove all drawings" onClick={() => setConfirmClear(false)} className="rounded px-2 py-1 text-[10px] text-slate-300 hover:bg-slate-800">Cancel</button></div></div> : <button type="button" disabled={!hasDrawings || !onClear} onClick={() => setConfirmClear(true)} className="flex min-h-8 w-full items-center gap-2 rounded-md px-2 text-left text-[11px] text-rose-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="trash" className="h-3.5 w-3.5" />Remove All Drawings</button>}</div>}</div>
  </aside>
}

function ToolCell({ label, mainTitle, active, main, onClick, onMenu, menu }: { label: string; mainTitle?: string; active: boolean; main: string; onClick: () => void; onMenu?: () => void; menu?: ReactNode }) { return <div className="group relative flex h-8 w-11 shrink-0 items-center rounded-md hover:bg-slate-800"><button type="button" aria-label={`${label} tools`} aria-pressed={active} title={mainTitle ?? label} data-tooltip={mainTitle ?? label} onClick={onClick} className={`${toolButton} ${onMenu ? 'flex-1' : 'mx-auto'} ${active ? 'bg-slate-800 text-slate-100' : ''}`}><Icon name={main} className="h-4 w-4" /></button>{onMenu && <button type="button" aria-label={`Show ${label} menu`} title={`Show ${label} menu`} onClick={onMenu} className="grid h-8 w-3 place-items-center rounded-r-md text-slate-500 opacity-0 transition hover:bg-slate-700 hover:text-slate-100 focus-visible:ring-2 focus-visible:ring-slate-300 group-hover:opacity-100 group-focus-within:opacity-100"><Icon name="chevronRight" className="h-2 w-2" /></button>}{menu}</div> }

export function ChartUtilities({ hasChart, onRefresh, refreshing = false, onSendAssistant }: { hasChart: boolean; onRefresh?: () => void; refreshing?: boolean; onSendAssistant?: (blob: Blob) => Promise<void> }) {
  const menu = useRef<HTMLDetailsElement>(null); const [status, setStatus] = useState('')
  const run = async (action: 'download' | 'copy' | 'assistant') => { setStatus('Preparing chart…'); try { const svg = document.querySelector<SVGSVGElement>('[data-chart-export] svg'); if (!svg) throw new Error('No chart is available to capture.'); const bounds = svg.viewBox.baseVal; const region = { x: 0, y: 0, width: bounds.width || 1200, height: bounds.height || 675 }; const blob = await captureSvgRegion(svg, region); if (action === 'download') { const url = URL.createObjectURL(blob), link = document.createElement('a'); link.href = url; link.download = 'quant-chart.png'; link.click(); URL.revokeObjectURL(url); setStatus('PNG downloaded.') } else if (action === 'copy') { if (!navigator.clipboard?.write || typeof ClipboardItem === 'undefined') throw new Error('Image clipboard is unavailable in this browser.'); await navigator.clipboard.write([new ClipboardItem({ 'image/png': blob })]); setStatus('Chart copied.') } else if (onSendAssistant) { await onSendAssistant(blob); setStatus('Chart sent to Assistant.') } else { await sendChartCaptureToAssistant({ blob, prompt: 'Please inspect this full active chart and explain the visible evidence.', context: { symbol: 'unknown', provider: 'COINBASE', timeframe: 'unknown', visibleTimeRange: { from: '', to: '' }, capturedTimeRange: { from: '', to: '' }, approximateCapturedPriceRange: { lower: 0, upper: 0 }, currentPrice: 0, selectedDrawingIds: [] }, region, fullChart: true }) }; menu.current?.removeAttribute('open') } catch (error) { setStatus(error instanceof Error ? error.message : 'Chart capture failed.') } }
  return <div data-testid="chart-utility-cluster" className="flex items-center gap-1">{status && <span role="status" className="hidden max-w-40 truncate text-[10px] text-slate-500 lg:inline">{status}</span>}{onRefresh && <button type="button" aria-label="Refresh datasets" title="Refresh datasets" data-tooltip="Refresh" disabled={refreshing} onClick={onRefresh} className={toolButton}><Icon name="refresh" className={`h-4 w-4 ${refreshing ? 'animate-spin' : ''}`} /></button>}<details ref={menu} className="relative"><summary aria-label="Chart capture and export" title="Capture" data-tooltip="Camera" className={`${toolButton} list-none cursor-pointer`}><Icon name="camera" className="h-4 w-4" /></summary><div className="absolute right-0 top-10 z-40 w-56 rounded-lg border border-slate-700 bg-slate-900 p-1.5 shadow-2xl"><p className="px-2 pb-1 pt-1 text-[10px] font-semibold uppercase tracking-[.12em] text-slate-600">Chart capture</p><button type="button" aria-label="Send full chart to Assistant" disabled={!hasChart} onClick={() => void run('assistant')} className="flex min-h-9 w-full items-center gap-2 rounded-md px-2 text-left text-xs text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="chat" className="h-4 w-4" />Send full chart to Assistant</button><button type="button" aria-label="Save PNG" disabled={!hasChart} onClick={() => void run('download')} className="flex min-h-9 w-full items-center gap-2 rounded-md px-2 text-left text-xs text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="download" className="h-4 w-4" />Save PNG</button><button type="button" aria-label="Copy chart image" disabled={!hasChart} onClick={() => void run('copy')} className="flex min-h-9 w-full items-center gap-2 rounded-md px-2 text-left text-xs text-slate-300 hover:bg-slate-800 disabled:text-slate-600"><Icon name="copy" className="h-4 w-4" />Copy chart image</button></div></details></div>
}
